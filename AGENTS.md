# AGENTS.md

このリポジトリはNeovim設定を管理する。

## 基本方針

- 設定変更は、検証後に原則として自動でコミットし、`origin main`へpushする。
- コミットメッセージは日本語で、変更内容を具体的に書く。
- push前に差分とLuaの構文を確認する。
- `git push --force`、`reset --hard`、無関係なファイルの削除は行わない。
- 既存の未コミット変更は保護し、担当範囲だけを明示的にstageする。

## 安全確認

- APIキー、パスワード、秘密鍵、SSH接続情報、個人のIPアドレスをコミットしない。
- ユーザー名やホームディレクトリなど、公開不要な個人情報はプレースホルダーへ置き換える。
- 個人情報や認証情報を含む可能性がある場合はpushを止め、ユーザーへ報告する。

## 検証

変更したLuaを`nvim --headless '+lua assert(loadfile("ファイルパス"))' +qa`で読み込めることを確認してからcommitする。
